// function takes in state and action, initial state has to be defined,
export default function (state = {}, action) {
    console.log(action);
    switch (action.type) {
        default:
            return state;
    }

}